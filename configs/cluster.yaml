app_name: "MSBX5420 Final Project"
env: "cluster"

paths:
  rraw: "s3://your-bucket-name/credit-card-transactions/credit_card_transactions.csv" #PLACEHOLDER
  processed: "s3://YOUR-BUCKET/processed"
  outputs_tables: "s3://YOUR-BUCKET/outputs/tables"
  outputs_figures: "outputs/figures"   # figures often generated locally after download

spark:
  master: null                          # cluster provides this
  shuffle_partitions: 200